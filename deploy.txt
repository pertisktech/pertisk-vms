# New Proxmox host (no VM 901 yet): create the appliance from out/pertisk-node.raw
#   sudo ./scripts/create-appliance-vm.sh 901
# Then deploy current binaries (also starts the VM):
#   sudo ./deploy.sh
#
# Deploy pertisk code updates to appliance VM 901 on this Proxmox host.
# Run on the Proxmox host — NOT inside the appliance (no Rust there).
#
# Only needed when you change pertisk source code — NOT after every reboot.

sudo ./deploy.sh

# UI: https://10.1.1.174:7443/  or http://10.1.1.174:7480/  login admin / admin
#
# Do NOT run pertisk-install inside the Proxmox VM — you are already booted from disk.
# pertisk-install is for copying the live image onto bare-metal NVMe/USB.
#
# If deploy was interrupted (mount left active, VM won't boot / fsck errors):
#   qm stop 901
#   umount /mnt/pertisk901 /mnt/pertisk-deploy-901 2>/dev/null; sync
#   e2fsck -f -y /dev/zvol/rpool/data/vm-901-disk-0-part2
#   fsck.vfat -a /dev/zvol/rpool/data/vm-901-disk-0-part1
#   qm start 901
