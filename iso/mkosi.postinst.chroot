#!/bin/bash
# Packages are installed but initrd does not exist yet (mkosi runs depmod
# after this). Only create mount points; pertisk-esp-boot runs in finalize.
set -euo pipefail
mkdir -p /efi /boot/efi
exit 0
